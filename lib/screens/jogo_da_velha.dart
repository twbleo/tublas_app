import 'package:flutter/material.dart';

class JogoDaVelhaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Jogo da Velha")),
      body: Center(child: Text("Aqui vai o jogo da velha!")),
    );
  }
}
